[app]
# Staging environment overrides
name = ML Orchestrator (Staging)

[server]
workers = 4  # Multiple workers for staging

[paths]
log_dir = /var/log/ml-orchestrator/staging

[logging]
level = INFO
max_size = 50  # MB
backup_count = 10

[external_services]
# Staging endpoints
model_service_url = https://api-stg.example.com/models
storage_service_url = https://storage-stg.example.com
