# Local Development Configuration for ML Orchestrator

[app]
debug = true

[paths]
# Use the local models directory for development
config_dir = config
models_dir = config/local/models

[logging]
level = DEBUG
format = %(asctime)s - %(name)s - %(levelname)s - %(message)s
